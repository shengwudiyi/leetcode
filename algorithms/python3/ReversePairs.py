# -*- coding: utf-8 -*-

nums = [1, 3, 2, 3, 1]


stack = []
for num in nums:
    stack.append([num])


new_stack = []
while len(stack) > 1:
    a = stack.pop()
    b = stack.pop()
    while i > 0 || j > 0:

